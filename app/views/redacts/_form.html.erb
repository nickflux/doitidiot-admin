<%= form_for @redact do |f| %>
  <%= f.label :code_name %>:
  <%= f.text_field :code_name %><br />
  <%= f.label :redact_array %>:<br/>
  <%= text_area_tag 'redact[redact_array]', @redact.redact_array.join("\n") %><br/>
  <%= f.submit %>
<% end %>
